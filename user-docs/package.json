{
  "name": "youtradecs-user-docs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev --host 0.0.0.0",
    "build": "vitepress build",
    "preview": "vitepress preview --host 0.0.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.12",
    "vite": "^6.4.3",
    "vitepress": "^1.6.4"
  },
  "overrides": {
    "esbuild": "$esbuild",
    "vite": "$vite"
  }
}
