# YouTrade CS2 AutoTrade Telegram Service

<div align="center">

![Java](https://img.shields.io/badge/Java-17-orange?style=for-the-badge&logo=openjdk)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.5.7-brightgreen?style=for-the-badge&logo=spring)
![Maven](https://img.shields.io/badge/Maven-3.8+-red?style=for-the-badge&logo=apache-maven)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-42.7.3-blue?style=for-the-badge&logo=postgresql)
![Redis](https://img.shields.io/badge/Redis-Streams-red?style=for-the-badge&logo=redis)
![Telegram Bot API](https://img.shields.io/badge/Telegram%20Bot%20API-9.2.0-blue?style=for-the-badge&logo=telegram)

**Telegram-бот для автоматического трейдинга предметов Counter-Strike 2 с интуитивным интерфейсом на основе inline-кнопок**

[Features](#-функциональность) • [Architecture](#-архитектура) • [Tech Stack](#-технологический-стек) • [Quick Start](#-быстрый-старт)

</div>

---

## 📋 Описание проекта

**YouTrade CS2 AutoTrade Telegram Service** — это высокопроизводительный Telegram-бот для управления системой автоматического трейдинга предметов Counter-Strike 2. Проект реализует полнофункциональный интерфейс управления параметрами автоматической покупки и продажи через систему inline-кнопок, обеспечивая удобное взаимодействие пользователей с торговой системой.

### 🎯 Ключевые особенности

- 🔄 **Автоматический трейдинг** — настройка параметров автоматической покупки и продажи CS2 предметов
- 📊 **Управление параметрами** — создание, переключение, копирование и удаление наборов параметров
- 🎯 **Система скоринга** — настройка оценки предметов для оптимизации торговых решений
- 🔍 **Фильтрация** — работа с включенными и исключенными ключевыми словами
- 📈 **Аналитика** — просмотр статистики, истории продаж и списков предметов
- 👥 **Социальные функции** — следование за другими пользователями и копирование их параметров
- ⚡ **Высокая производительность** — многопоточная обработка через Redis Streams

## 🚀 Функциональность

### Основные функции

- ✅ Управление наборами параметров трейдинга (создание, переключение, удаление, переименование)
- ✅ Настройка параметров автоматической покупки (AutoBuy) с различными режимами
- ✅ Настройка параметров автоматической продажи (AutoSell) с гибкой системой оценки цен
- ✅ Управление системой оценки предметов (скоринг) для оптимизации торговли
- ✅ Фильтрация по ключевым словам (включенные/исключенные слова)
- ✅ Управление токенами для торговых операций (Steam API, Trade URL)
- ✅ Просмотр статистики и списков предметов за различные периоды
- ✅ Настройка следования за другими пользователями с копированием параметров
- ✅ Работа с таблицами данных для расчета цен продажи
- ✅ Административные функции для управления пользователями и балансом

### Архитектура интерфейса

Интерфейс реализован как иерархическая система состояний с inline-кнопками. Каждое меню представлено отдельным состоянием, наследующимся от `AbstractMenuState`. Состояния управляют отображением кнопок и обработкой действий пользователя. Состояния пользователей хранятся в памяти через `ConcurrentHashMap<UserData, UserStateData>`.

**Иерархия меню:**
- 🏠 Стартовое меню (`UserStartState`) — начальная точка входа
- 📱 Главное меню (`UserMainState`) → ⚙️ Параметры (`UserParamsState`) → AutoBuy/AutoSell/Follow/Токены
- 🤖 AutoBuy: настройка параметров, скоринг, ключевые слова
- 💰 AutoSell: настройка параметров, таблицы данных (продажа, ожидание, история, загрузка, изменение, ограничения)
- 📋 Дополнительные функции: список параметров, переключение, создание, удаление, переименование

## 📨 Получение сообщений

Проект получает входящие сообщения от Telegram через **Redis Streams**. `TelegramRedisStreamConsumerService` подписывается на Redis Stream (`telegram-updates-alt`), обрабатывает сообщения в многопоточном режиме и передает их в `TelegramUpdReceiverService` для дальнейшей обработки. Используется consumer group для распределенной обработки и механизм claim для обработки зависших сообщений.

## 🔄 Обработка взаимодействий

### Callback-запросы

Обработка нажатий на inline-кнопки происходит через callback-запросы Telegram Bot API. Каждый callback содержит идентификатор пункта меню, который преобразуется в enum-значение и обрабатывается соответствующим состоянием.

### Построение клавиатуры

Клавиатура формируется автоматически на основе enum-значений меню. Каждый enum содержит текст кнопки и используется как callback-данные.

### Отправка сообщений

Реализована очередь сообщений с rate limiting:
- 📬 Внутренняя очередь: `BlockingQueue<MessageInfoDto>`
- ⏱️ Ограничение частоты: минимум 1020 мс между сообщениями для одного пользователя
- 📝 Типы сообщений: TEXT, DOCUMENT, EDIT, ANSWER_CALLBACK
- 🚀 Отправка через `ScheduledThreadPoolExecutor` с интервалом 35 мс

## 🔌 Интеграция с backend

Взаимодействие с бизнес-логикой осуществляется через REST API endpoints. HTTP-клиент реализован на Apache HttpClient 5.

### Иерархия эндпоинтов

```
AbstractAtEndpoint (базовый класс)
├── GeneralEndpoint (/api/telegram/user/general)
│   └── Получение информации об аккаунте, данных за период, управление токенами, изменение полей
├── ParamsEndpoint (/api/telegram/user/params)
│   └── Создание, получение, переключение и удаление параметров, копирование и следование
├── BuyEndpoint (/api/telegram/user/buy)
│   └── Управление токенами для покупки, переключение режимов автопокупки
├── AbstractAtWordsEndpoint (абстрактный класс для работы со словами)
│   ├── IncludedWordsEndpoint (/api/telegram/user/buy/words/included)
│   └── ExcludedWordsEndpoint (/api/telegram/user/buy/words/excluded)
├── ProfitWordsEndpoint (/api/telegram/user/buy/profit)
│   └── Управление параметрами профита (добавление, редактирование, удаление)
├── SellDefaultEndpoint (/api/telegram/user/sell)
│   └── Переключение режимов автопродажи, получение истории и ожидающих продаж
├── SellListEndpoint (/api/telegram/user/sell/list)
│   └── Получение и отправка списков предметов для продажи
├── SellTokensAddEndpoint (/api/telegram/user/sell/token)
│   └── Добавление токенов для продажи
├── SellUploadEndpoint (/api/telegram/user/sell/upload)
│   └── Получение и отправка загруженных предметов
├── SellChangeEndpoint (/api/telegram/user/sell/change)
│   └── Получение и отправка изменений цен, группировка изменений
├── SellRestrictEndpoint (/api/telegram/user/sell/restrictions)
│   └── Управление ограничениями на продажу
├── AtAdminEndpoint (/api/telegram/admin)
│   └── Административные функции: управление пользователями, балансом, удаление подписок
├── SubGetEndpoint (/api/telegram/no-role/sub)
│   └── Получение информации о подписке без роли
└── AbstractAtNoRoleAddEndpoint (абстрактный класс для добавления без роли)
    ├── NoRoleChatAddEndpoint (/api/telegram/no-role/chat)
    └── NoRoleAdminAddEndpoint (/api/telegram/no-role/admin)
```

### Архитектура создания эндпоинтов

Все эндпоинты построены на основе иерархии абстрактных классов из директории `parent`:

**Базовый класс `AbstractAtEndpoint`:**
- Реализует интерфейс `AtCommunicationInt` с методом `getMainEndpoint()`
- Содержит инициализацию HTTP-клиента `YtSyncRestClient` через `@PostConstruct`
- Управляет заголовками запросов через `InnerKeyManagerService` (добавление `X-Service-Key`)
- Предоставляет методы `createEndpoint()` для построения полных URL
- Использует конфигурацию `yt.message.endpoint` из `application.properties`

**Специализированные абстрактные классы:**

- `AbstractAtWordsEndpoint` - расширяет `AbstractAtEndpoint`, предоставляет методы для работы со словами:
  - `wordsGet()` - получение списка слов
  - `wordsAdd()` - добавление слов
  - `deleteWords()` - удаление слов по ID
  - `deleteAllWords()` - удаление всех слов
  Используется `IncludedWordsEndpoint` и `ExcludedWordsEndpoint` для работы с включенными и исключенными ключевыми словами.

- `AbstractAtNoRoleAddEndpoint` - расширяет `AbstractAtEndpoint`, предоставляет метод:
  - `save()` - сохранение токена без роли
  Используется `NoRoleChatAddEndpoint` и `NoRoleAdminAddEndpoint` для регистрации пользователей без роли.

**Создание нового эндпоинта:**

1. Наследование от `AbstractAtEndpoint` или специализированного абстрактного класса
2. Реализация метода `getMainEndpoint()`, возвращающего базовый путь API
3. Добавление методов для конкретных операций с использованием `client.fetchFromApi()`
4. Аннотация `@Component` для регистрации в Spring контексте

HTTP-клиент реализован на Apache HttpClient 5.

## 🛠 Технологический стек

### Язык и платформа
- **Java 17** — основной язык разработки
- **Spring Boot 3.5.7** — фреймворк приложения
- **Maven** — система сборки проекта

### Telegram
- **telegrambots-client 9.2.0** — клиент для Telegram Bot API
- **telegrambots-extensions 9.2.0** — расширения для Telegram Bot API

### Базы данных и кэширование
- **Spring Data Redis** — интеграция с Redis для работы со Streams
- **Spring Data JPA** — работа с реляционными базами данных
- **PostgreSQL 42.7.3** — основная база данных

### HTTP и сериализация
- **Apache HttpClient 5** (httpclient5 5.4-alpha2, httpcore5 5.3-alpha2, httpcore5-h2 5.3-alpha2) — HTTP-клиент
- **Gson 2.10.1** — JSON сериализация/десериализация
- **Jackson XML 2.14.2** — работа с XML форматом

### Утилиты
- **Lombok 1.18.34** — уменьшение boilerplate кода
- **Apache Commons Text 1.12.0** — работа с текстом
- **Apache POI 5.4.1** — работа с Excel файлами (XLSX)

## 📁 Структура проекта

```
youtrade.cs-autotrade-telegram-service/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── cs/youtrade/autotrade/client/
│   │   │       ├── Main.java                          # Точка входа приложения
│   │   │       ├── telegram/                          # Telegram-бот функциональность
│   │   │       │   ├── menu/                          # Меню и состояния
│   │   │       │   │   ├── main/                      # Главное меню
│   │   │       │   │   │   ├── create/                # Создание параметров
│   │   │       │   │   │   ├── delete/                # Удаление параметров
│   │   │       │   │   │   ├── items/                 # Получение новых предметов
│   │   │       │   │   │   ├── list/                  # Список параметров
│   │   │       │   │   │   ├── params/                # Настройки параметров
│   │   │       │   │   │   │   ├── autobuy/           # Настройки AutoBuy
│   │   │       │   │   │   │   ├── autosell/          # Настройки AutoSell
│   │   │       │   │   │   │   ├── follow/            # Настройки Follow
│   │   │       │   │   │   │   ├── token/             # Управление токенами
│   │   │       │   │   │   │   └── rename/            # Переименование
│   │   │       │   │   │   └── pswitch/               # Переключение параметров
│   │   │       │   │   ├── start/                     # Стартовое меню
│   │   │       │   │   └── UserMenu.java              # Enum всех состояний
│   │   │       │   ├── messaging/                     # Сервисы сообщений
│   │   │       │   │   ├── redis/                     # Redis Streams consumer
│   │   │       │   │   ├── TelegramSendMessageService.java
│   │   │       │   │   └── TelegramUpdReceiverService.java
│   │   │       │   └── prototype/                     # Базовые классы
│   │   │       │       ├── menu/                      # Базовые классы меню
│   │   │       │       ├── state/                     # Регистрация состояний
│   │   │       │       └── sender/                    # Отправка сообщений
│   │   │       └── util/                              # Утилиты
│   │   │           ├── autotrade/
│   │   │           │   ├── communication/             # HTTP-клиент
│   │   │           │   ├── dto/                       # Data Transfer Objects
│   │   │           │   └── endpoint/                  # REST API endpoints
│   │   │           │       ├── admin/                 # Административные endpoints
│   │   │           │       ├── norole/                # Endpoints без роли
│   │   │           │       ├── parent/                # Базовые классы endpoints
│   │   │           │       └── user/                  # Пользовательские endpoints
│   │   │           └── datasource/                    # Конфигурация БД
│   │   └── resources/
│   │       └── application.properties                 # Конфигурация
│   └── test/                                           # Тесты
├── pom.xml                                             # Maven конфигурация
└── README.md                                           # Документация
```

## 🚀 Быстрый старт

### Требования

- Java 17 или выше
- Maven 3.8+
- PostgreSQL 12+
- Redis 6.0+ (с поддержкой Streams)
- Telegram Bot Token

## 🏗 Реализация

### Паттерны проектирования
- **State Pattern** — для управления состояниями меню
- **Abstract Factory** — для создания состояний
- **Builder** — для построения сообщений и запросов
- **Template Method** — в базовых классах эндпоинтов

### Обработка данных
- Enum-based типизация пунктов меню
- DTO для передачи данных между слоями
- Централизованная обработка ошибок
- Gson для десериализации сообщений из Redis Streams

### Многопользовательская поддержка
- Изоляция состояния по пользователям через `UserData`
- Регистрация состояний через `StateRegistry`
- Потокобезопасные структуры данных (`ConcurrentHashMap` для хранения состояний)
- Многопоточная обработка сообщений из Redis Streams через пул воркеров

### Обработка обновлений
- `TelegramRedisStreamConsumerService` — получение обновлений из Redis Streams
- `TelegramUpdReceiverService` — маршрутизация обновлений к соответствующим состояниям
- Автоматическое удаление обработанных сообщений из Redis Stream
- Обработка зависших сообщений через механизм claim

## 📊 Производительность

- ⚡ Многопоточная обработка через пул воркеров Redis Streams
- 🚀 Rate limiting для соблюдения ограничений Telegram API
- 💾 Эффективное хранение состояний в памяти
- 🔄 Автоматическая обработка зависших сообщений

## 🔍 SEO Keywords

**Telegram Bot**, **CS2 Trading**, **AutoTrade**, **Spring Boot**, **Java Telegram Bot**, **CS2 Items Trading**, **Counter-Strike 2**, **Redis Streams**, **Telegram Bot API**, **Steam Trading**, **CS2 AutoTrade Bot**, **Telegram Trading Bot**, **Spring Boot Telegram**, **Redis Consumer**, **CS2 Market Bot**

---

<div align="center">

*Примечание: Этот README был сгенерирован с помощью AI-ассистента 😊. Использование AI помогает структурировать техническую информацию более понятным и логичным образом для читателя, что особенно важно при документировании сложных систем. Финальный текст формируется с вниманием и ответственностью.*

</div>
